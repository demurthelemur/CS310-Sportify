import './location.dart';

class SearchResult {
  final String userName;
  final String profilePicture;
  final Location location;

  SearchResult(this.userName, this.profilePicture, this.location);
}